# Eyot PostgreSQL Deployment for OrbStack.
#
# Wave 1 of P14b: OrbStack-ready PostgreSQL with local-image pull policy
# (OrbStack shares the host Docker daemon) and a pg_isready readiness probe.
# Shared by backend + portal pods running in the same cluster.
---
apiVersion: v1
kind: Namespace
metadata:
  name: cocoa
  labels:
    name: cocoa
---
apiVersion: v1
kind: Secret
metadata:
  name: cocoa-postgres-secret
  namespace: cocoa
type: Opaque
stringData:
  POSTGRES_DB: cocoa
  POSTGRES_USER: postgres
  POSTGRES_PASSWORD: cocoa_dev_password
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cocoa-postgres-data
  namespace: cocoa
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: cocoa-postgres
  namespace: cocoa
  labels:
    app: cocoa-postgres
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cocoa-postgres
  template:
    metadata:
      labels:
        app: cocoa-postgres
    spec:
      containers:
        - name: postgres
          image: postgres:16.4-alpine
          imagePullPolicy: Never
          envFrom:
            - secretRef:
                name: cocoa-postgres-secret
          ports:
            - containerPort: 5432
          readinessProbe:
            exec:
              command: ["pg_isready", "-U", "postgres"]
            initialDelaySeconds: 5
            periodSeconds: 5
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: cocoa-postgres-data
---
apiVersion: v1
kind: Service
metadata:
  name: cocoa-postgres
  namespace: cocoa
spec:
  type: ClusterIP
  selector:
    app: cocoa-postgres
  ports:
    - port: 5432
      targetPort: 5432