services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    ports:
      - "5433:5432"
    environment:
      POSTGRES_USER: cocoa
      POSTGRES_PASSWORD: cocoa
      POSTGRES_DB: cocoa_dev
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
